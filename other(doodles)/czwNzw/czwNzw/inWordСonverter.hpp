#ifndef inWordConverter_hpp
#define inWordConverter_hpp

#include <stdio.h>
#include <string>
using namespace std;

string singleDigit(int number);
string twoDigit(int number);
string threeDigit(int number);
string fourDigit(int number);

#endif
